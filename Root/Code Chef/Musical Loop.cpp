#include <stdio.h>

enum {up , down , equal};

int main()
{
    int prev,now,cnt,n,i,j,k;
    while(scanf("%d",&n) == 1 && n)
    {
        cnt = 0;
        prev = now = 3;

    }

    return 0;
}
